export type  Language = 'en' | 'hu';
export type Theme = 'light' | 'dark';

export type Translations = {
  header: {
    search: string;
    button: string;
  };
  subheader: {
    walletOptions: string;
    clearBtn: string;
  };
  
 // Later :
  // footer?: {
  //   copyright: string;
  // };
  // form?: {
  //   submit: string;
  //   cancel: string;
  // };
};

